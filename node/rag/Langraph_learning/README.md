# The Real Purpose of LangGraph

It is not another LLM.

It is not another agent.

It is an execution engine.

Its job is to answer questions like:

Which node runs first?
Which node runs next?
Should we call a tool?
Should we loop back?
Should we stop?
Which agent should execute now?
Should we wait for human approval?
Should we retry after an error?